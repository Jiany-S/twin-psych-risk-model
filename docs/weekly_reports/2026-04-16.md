# 2026-04-16

## Hypothesis
Improving subject-holdout split quality (especially validation positive-window coverage) should stabilize threshold selection and increase stress metrics without changing model architecture.

## Config Delta
- Base full run: `experiments/runs/20260416_135957` using `src/config/multiphysio_full.yaml` with initial split builder.
- New run: `experiments/runs/20260416_140806` using the same config family but upgraded split builder in `scripts/run_multiphysio_full.py`:
  - deterministic assignment balancing worker count + rows + positive windows
  - enforced minimum positives:
    - `split.min_val_positive_windows: 20`
    - `split.min_test_positive_windows: 20`

## Key Metrics Delta (Stress, Test)
- XGBoost:
  - AUROC: `0.373 -> 0.480` (`+0.107`)
  - AUPRC: `0.185 -> 0.562` (`+0.377`)
  - F1: `0.000 -> 0.639` (`+0.639`)
  - ECE: `0.178 -> 0.011` (`-0.167`)
- TFT:
  - AUROC: `0.227 -> 0.695` (`+0.468`)
  - AUPRC: `0.126 -> 0.806` (`+0.680`)
  - F1: `0.000 -> 0.658` (`+0.658`)
  - ECE: `0.232 -> 0.545` (`+0.313`, worse)

## Split/Distribution Notes
- Previous test class balance: `{'0.0': 110, '1.0': 25}`.
- New test class balance: `{'0.0': 77, '1.0': 96}`.
- Interpretation: metric gains are substantial, but prevalence shifted significantly; this run is better for train/val/test class validity, not yet a final benchmark.

## Decision
Needs follow-up.

## Next Step
Hold split policy fixed (new builder), then tune a single variable family: `dataset.multiphysio.stress_threshold` (e.g., sweep `35/40/45`) to improve calibration and preserve TFT AUROC while reducing TFT ECE.
